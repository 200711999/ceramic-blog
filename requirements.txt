# 核心依赖
Django==4.2.10
python-dotenv==1.2.2
Pillow==12.2.0
requests==2.33.1
bleach==6.4.0

# 数据库驱动（二选一）
# mysqlclient 性能更好，但在 Windows / 部分 Linux 上需要额外编译依赖，容易安装失败
# 如果已安装 MySQL/MariaDB 开发库，可取消下面注释使用 mysqlclient
# mysqlclient==2.2.8
# PyMySQL 是纯 Python 实现，开箱即用，推荐开发和跨平台使用
PyMySQL==1.1.3

# Redis 缓存
django-redis==5.4.0
